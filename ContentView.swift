//
//  ContentView.swift
//  We_Move
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("We_Move")
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
